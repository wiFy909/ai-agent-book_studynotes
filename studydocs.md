# 学习笔记

## 当前学习状态

- **最后更新：** 2026-07-30
- **当前章节：** 第 1 章《AI Agent 入门》，进行中
- **已记录主题：** 7 组
- **代码实践状态：** 本次提交尚未形成新的实验验收记录
- **首页进度：** [返回 README](README.md#-当前精读进度)

## 笔记结构

每个主题都沿用同一条阅读链，避免把原作内容、个人理解和后续验证混在一起：

1. **原文**：保留原作表述或结构。
2. **来源**：链接到仓库内的正文位置。
3. **我的心得**：记录个人理解、经验对照或疑问。

需要说明认识所处的阶段时，使用以下标记：

| 标记 | 含义 |
| --- | --- |
| `高价值` | 已理解其机制和适用条件，值得迁移到其他项目 |
| `经验对照` | 与个人模型、Agent 或 Vibe Coding 经历形成对照 |
| `待实践` | 已阅读，但尚未通过配套代码或真实项目观察 |
| `待验证` | 个人推演、疑问或尚无充分来源支撑的判断 |

> [!IMPORTANT]
> 这些标记只评价我的当前理解状态，不评价原作质量。个人心得中的推演也不代表原作者结论。

## 第 1 章：AI Agent 入门

### 1. 工具设计思维

#### 原文

> [在为 Agent 设计工具时，可以先从任务所需的最窄能力起步，再随任务复杂度提升逐步扩展。](book/chapter1.md#L99)

**来源：** [`book/chapter1.md:99`](book/chapter1.md#L99)

#### 我的心得

> 这句话提醒我，Agent 工具设计本身就是一个通用能力摸索的过程，最后的趋势就是高阶智能模型的工具变为最原始的编译器，然后*使用超级量子计算机快速构建一切工具*，然后调用构建的工具来完成用户的任务。

---

### 2. Harness 概念

#### 原文

> [“模型即 Agent”（Model as Agent）这一新范式代表了 AI Agent 发展的最新方向。先进模型通过后训练（特别是强化学习）将工具调用能力内化为原生能力：何时调用工具、调哪个、传什么参数，都由模型自己决定，无需人工编排。但这并不意味着框架层变得不重要了。恰恰相反，模型越强大，围绕模型构建的 Harness 就越关键。](book/chapter1.md#L117)

**来源：** [`book/chapter1.md:117`](book/chapter1.md#L117)

#### 我的心得

> 1. Harness 是通过强化学习让模型知道调用什么工具最合适；是*深度学习取代强化学习后、强化学习的一次反扑*。
> 2. Harness 是模型本身智能进阶的产物，也只是中间阶段，**终点就是单一模型生万物**。

---

### 3. Agent 的学习机制

#### 原文

> [前面讨论了模型可以通过强化学习将工具调用策略内化为原生能力。但 Agent 的行为改变不只发生在训练阶段。按照更新发生的位置和持续时间，可以把它理解为三条互补路径。](book/chapter1.md#L127)

**来源：** [`book/chapter1.md:127`](book/chapter1.md#L127)

#### 我的心得

> 1. **上下文适应**就是最基础的但不能记忆的让模型调用最匹配上下文的知识。
> 2. **外部制品**就是从多个问答中提取固化后的答题套路或范式，具体表现为默认 prompt、skills。
> 3. **模型参数**就是直接根据业务需求调模型内部参数，让模型内部直接偏向特定领域。

---

### 4. Agent 的回复驱动因素组合

#### 原文

> [前两项（系统提示词 + 工具定义）是静态前缀，后三项（用户消息 + 模型回复 + 工具执行结果）是随交互不断增长的动态消息历史。这五个部分共同构成了 LLM 每次推理时的上下文。](book/chapter1.md#L147)

**来源：** [`book/chapter1.md:147`](book/chapter1.md#L147)

#### 我的心得

> 五个部分（*二静三动*）就是 Agent 类智能体生成用户想要的答案的支撑因素和驱动过程。

---

### 5. ReAct 循环

这一主题包含两组相互衔接的“原文—心得”：先理解 ReAct 轨迹，再理解强化学习怎样改变工具调用的决策位置。

#### A. ReAct 轨迹怎样形成循环

**原文**

> [静态前缀对应前文五个组件中的前两项（系统提示词 + 工具定义），轨迹对应后三项（用户消息 + 模型回复 + 工具执行结果，随交互不断增长）。基于这个完整上下文，LLM 生成下一步的响应，然后这个响应又追加到轨迹中，供下一次调用使用。](book/chapter1.md#L167)
>
> [实际循环包含三个环节：模型先**思考**当前应该做什么，然后调用工具**行动**，再**观察**工具返回的结果并继续思考下一步。这个“想→做→看→想→做→看”的循环不断重复，直到任务完成。](book/chapter1.md#L165)

**来源：** [`book/chapter1.md:165-167`](book/chapter1.md#L165-L167)

**我的心得**

> 1. 轨迹就是循环渐进的上下文。
> 2. Agent 通过轨迹保持着对整个任务的全局认知。
> 3. 轨迹包括的*三动*是结构化的，便于审计分析。

#### B. 强化学习改变决策位置

**原文**

> [RL 优化的是决策策略，而不是把搜索引擎或代码沙盒“装进”模型的权重。因此编排循环并没有消失，而是从客户端移到了服务端，同时决策权交给了模型。](book/chapter1.md#L224)

**来源：** [`book/chapter1.md:224`](book/chapter1.md#L224)

**我的心得**

> 表现出色的模型就是靠 RL 教会模型如何高效、正确使用工具来得到高质量的回答。

#### 补充理解：RL、神经网络和工具调用训练

RL 的英文全称是 **Reinforcement Learning**，中文叫**强化学习**。它不是一种神经网络结构，而是一种训练方法：模型在已有环境、工具和动作空间中反复尝试，根据最终结果和过程表现获得奖励；训练会提高高奖励行为序列的概率，降低低奖励行为序列的概率。神经网络则是承载这些策略的参数化模型，深度学习是用多层神经网络进行表示学习和参数优化的方法集合，强化学习可以用神经网络实现，也可以不用神经网络实现。

> **数学化理解：** 神经网络可以近似看作一个高维非线性函数 `f_theta(x)`：`x` 是输入，`theta` 是参数，输出可以是答案 `y`，也可以是动作概率分布 `pi_theta(a|s)`。这里的“非线性”不是简单指“二次以上幂”，而是指函数中包含激活函数、注意力等非线性变换，不能被一个线性函数完整表示。

用一个比方说，神经网络像“大脑结构”，深度学习像“训练这颗大脑的通用方法体系”，强化学习像“通过奖惩训练行动策略”。在 Agent 场景中，工具本身仍由外部框架或 API 提供；模型通过 RL 学到的是决策策略：何时调用工具、调用哪个工具、参数怎么写、拿到结果后是否继续、何时停止并生成最终答案。最终固化到模型参数里的不是一条标准路径，而是“在什么状态下更可能采取什么行动”的策略倾向。

---

### 6. Harness 方法论

这一主题先整理 Harness 的五项功能，再用 Claude Code 的工程比例说明约束、验证与纠正为什么重要。

#### A. Harness 的五项功能

**原文**

| 功能 | 一句话职责 | 与上下文/工具的关系 | 核心原则 |
| --- | --- | --- | --- |
| **Context（上下文）** | 为模型提供感知信息 | 核心能力 | 信息充分性：让 Agent 在每个决策点都基于足够的信息判断 |
| **Tools（工具）** | 为模型提供行动手段 | 核心能力 | 接口清晰：工具命名直观、参数有例子、边界有说明 |
| **Constrain（约束）** | 设定行为边界——能做什么、不能做什么 | 围绕上下文和工具构建的安全边界 | 故障安全默认值：所有能力默认关闭，必须显式开放（类似手机 App 权限管理） |
| **Verify（验证）** | 自动判断操作结果的对错 | 围绕工具执行结果构建的检查机制 | 输入隔离：安全检查只看结构化数据（如工具返回的 JSON 字段），而不是模型自由生成的文本（因为攻击者可能通过提示注入操纵模型输出） |
| **Correct（纠正）** | 发现问题时自动修正或回退 | 围绕工具调用失败构建的恢复机制 | 在确认无法恢复之前，不暴露中间态（例如工具调用失败时先静默重试，不将半成品结果展示给用户） |

**来源：** 功能与职责见 [`book/chapter1.md:253-271`](book/chapter1.md#L253-L271)；核心原则见 [`book/chapter1.md:303-309`](book/chapter1.md#L303-L309)

#### B. Harness 的工程重心

**原文**

> [以 Claude Code 为例，它的 Harness 中绝大部分代码都是约束、验证与纠正，而非上下文与工具——工具本身（文件读写、命令执行、搜索）只是一小部分，而围绕这些工具构建的保障机制才是真正的核心。](book/chapter1.md#L277)

**来源：** [`book/chapter1.md:277`](book/chapter1.md#L277)

#### 我的心得

> 工程部分的原则构建和设计思路，是发挥高阶智能模型能力、使其达到生产级（高效、稳定、可用）的关键，这就是 Harness 命名的核心要义。

---

### 7. 工具接口设计技巧

#### 原文

> [ACI 强调的是从 Agent 视角设计接口（让 Agent 容易理解和使用），而非传统 API 从程序员视角设计接口。工具的命名和参数要直观，容易误用的地方要从设计上让错误无法发生——比如 SIM 卡的缺角让卡片只能从一个方向插进卡槽，避免了用户插反的错误；微波炉门没关好就绝不加热，避免了用户开门加热的危险行为。这种“用设计消除错误”的思路，在制造业里有一个专门的术语，叫**防呆**（Poka-yoke），源自丰田生产体系。设计不好的工具会让再强的模型也频繁出错——因为模型与工具之间唯一的沟通通道就是接口本身，模糊的接口会被模型放大成系统性的错误。](book/chapter1.md#L322)

**来源：** [`book/chapter1.md:322`](book/chapter1.md#L322)

#### 我的心得

> 给模型设计接口要规则化强。人可以主动思考接口的留白或不规范，对于模型来说就是危险或浪费的因素。
