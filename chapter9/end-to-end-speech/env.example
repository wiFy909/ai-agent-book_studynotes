# Exact Step-Audio R1 customized-vLLM server (see deploy_step_audio_r1.sh).
STEP_AUDIO_ENDPOINT=http://localhost:9999

# Only the ASR -> text-LLM comparison baseline uses OpenAI.
OPENAI_API_KEY=your_openai_api_key
